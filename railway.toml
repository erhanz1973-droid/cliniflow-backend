# Monorepo root — production API runs from cliniflow-backend-clean/ (set Root Directory in Railway UI).
# Do not use Nixpacks here; the live service uses cliniflow-backend-clean/Dockerfile.

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
