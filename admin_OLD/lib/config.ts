/**
 * Central API origin for the admin panel.
 * Override with NEXT_PUBLIC_API_URL in .env.local (see .env.example).
 */
export const API_URL =
  process.env.NEXT_PUBLIC_API_URL ||
  "https://cliniflow-backend-dg8a.onrender.com";
