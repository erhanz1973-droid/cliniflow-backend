/**
 * Canonical definitions live in `@cliniflow/procedures` (monorepo packages/procedures).
 * Run `npm install` from cliniflow-backend so `file:../packages/procedures` resolves.
 */
module.exports = require("@cliniflow/procedures");
